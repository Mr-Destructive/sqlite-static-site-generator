[build-system]
requires = ["flit_core >=2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "llm_gemini"
version = "0.1"
description = "LLM plugin for free Gemini API"
authors = [{name = "User"}]

[project.entry-points.llm]
gemini_free = "llm_gemini"
