{
  "name": "sqlite-markdown",
  "private": true,
  "type": "module",
  "dependencies": {
    "sql.js": "^1.7.0"
  },
  "scripts": {
    "vercel-build": "mkdir -p public/data && cp -R data/* public/data/"
  }
}
